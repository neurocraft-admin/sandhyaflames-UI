{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Angular Debug (Edge)",
      "type": "pwa-msedge",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
