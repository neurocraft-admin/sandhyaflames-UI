import { Routes } from '@angular/router';
import { VehicleAssignmentListComponent } from './vehicle-assignment-list.component';

export const routes: Routes = [
  { path: '', component: VehicleAssignmentListComponent }
];
