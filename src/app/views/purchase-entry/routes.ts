import { Routes } from '@angular/router';
import { PurchaseEntryListComponent } from '../purchase-entry/purchase-entry-list.component';

export const routes: Routes = [
  { path: '', component: PurchaseEntryListComponent }
];