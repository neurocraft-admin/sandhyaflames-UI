import { Routes } from '@angular/router';
import { ProductPricingComponent } from '../product-pricing/product-pricing.component';

export const routes: Routes = [
  { path: '', component: ProductPricingComponent }
];