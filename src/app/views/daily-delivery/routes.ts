import { Routes } from '@angular/router';
import { DailyDeliveryComponent } from '../daily-delivery/daily-delivery.component';

export const routes: Routes = [
  { path: '', component: DailyDeliveryComponent }
];